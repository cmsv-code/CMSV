# CMSV pip dependencies installed after creating environment_cmsv.yml.
#
# The GPU PyTorch stack is managed by conda:
#   pytorch=2.5.1
#   pytorch-cuda=12.1
#
# CUDA extensions are installed separately with source builds:
#   MAX_JOBS=8 python -m pip install --no-cache-dir --no-build-isolation --no-binary=causal-conv1d causal-conv1d==1.6.0
#   MAX_JOBS=8 python -m pip install --no-cache-dir --no-build-isolation --no-binary=mamba-ssm mamba-ssm==2.3.0
pysam==0.23.3
